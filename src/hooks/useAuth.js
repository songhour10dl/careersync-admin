// carrear-frontend-admin/src/hooks/useAuth.js

import { useAuth } from '../contexts/AuthContext';

/**
 * Custom hook to easily consume the authentication context.
 * Provides: user, isAuthenticated, role, loading, login, logout.
 */
export { useAuth };